"""clipcompose — manifest-driven video composition.

Compose annotated video clips into themed sections (spatial layout)
and assemble sections into a final video with transitions (temporal).
All content and layout is declared in YAML manifests.
"""
